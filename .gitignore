# Secrets / local config
.env
.kamal/secrets

# Kamal — config/deploy.yml holds committed defaults; real per-environment
# values live in config/deploy.<destination>.yml, which stays local.
config/deploy.*.yml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Test / tooling caches
.pytest_cache/

# macOS
.DS_Store
